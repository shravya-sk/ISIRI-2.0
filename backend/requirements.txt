annotated-types==0.7.0
anyio==4.14.2
click==8.4.2
colorama==0.4.6
fastapi==0.115.0
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic==2.9.2
pydantic-settings==2.6.0
pydantic_core==2.23.4
python-dotenv==1.0.1
python-multipart==0.0.32
PyYAML==6.0.3
starlette==0.38.6
typing_extensions==4.16.0
uvicorn==0.32.0
watchfiles==1.2.0
websockets==16.1.1


# --- Imported by the code but previously missing from this file, so a clean
# --- `pip install -r requirements.txt` produced a backend that could not start.
# --- Left unpinned deliberately: no known-good versions were recorded.

# backend/app/plugins/{weather,knowledge}.py
requests
# backend/app/main.py (pulls in torch - large download)
openai-whisper
# backend/app/voice/tts.py
edge-tts
pygame
# backend/app/plugins/alarm.py
APScheduler
dateparser
# ai_engine/translator.py
rapidfuzz
pandas
numpy

# Note: the door-lock plugin (backend/app/plugins/hardware.py) deliberately uses
# only the standard library, so the lock keeps working even if the heavier
# dependencies above are not installed.
